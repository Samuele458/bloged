{
  "name": "@bloged/root",
  "private": true,
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna exec --scope '{@bloged/blog,@bloged/dashboard}' -- yarn start",
    "stop": "kill-port 3000 3001",
    "build": "lerna exec --scope '{@bloged/blog,@bloged/dashboard}' -- yarn build"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "dependencies": {
    "kill-port": "^1.6.1"
  }
}
