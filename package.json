{
  "name": "@easyblog/root",
  "private": true,
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna exec --scope '{@easyblog/blog,@easyblog/api}' -- yarn start",
    "stop": "kill-port 3000 3001"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "dependencies": {
    "kill-port": "^1.6.1"
  }
}
