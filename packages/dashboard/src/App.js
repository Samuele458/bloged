import "@bloged/ui-components";

function App() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>{window.location.host}</p>
      <p>{window.location.protocol}</p>
    </div>
  );
}

export default App;
